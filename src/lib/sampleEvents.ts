import { LocalEvent } from '@/types/event';

// Sample events are no longer needed — all events come from scrapers.
// Keeping this file for the export interface used by useEvents.ts.
export const SAMPLE_EVENTS: LocalEvent[] = [];
